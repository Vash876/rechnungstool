{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm ci --only=production && npx prisma generate && cd frontend && npm ci --only=production && FORCE_COLOR=0 npm run build 2>&1",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm ci --only=production",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
